# MB RNN

```
apt-get install python3
apt-get install python3-pip
apt-get install git
apt-get install python3-venv
apt-get install postfix
(to see crontab output)

python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt

58 23 * * * sh /home/xxx/mbrnn/run.sh
```
